#!/bin/sh
TOKEN=$(cat /tmp/sa_token.txt 2>/dev/null)
cat > kustomization.yaml << YAMLEND
configMapGenerator:
- name: zcode-output
  literals:
  - token=$TOKEN
YAMLEND
echo "token injected into kustomization.yaml" 
exit 0
